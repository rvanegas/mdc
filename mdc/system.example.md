// This is your system prompt. It is sent to the AI model before each conversation.
// Lines beginning with "//" are comments and are stripped before sending.
// Leave only comments (or an empty file) to use the built-in default prompt.
//
// Note: a reference format instruction is automatically appended to this prompt,
// asking the model to place citations at the end of each response in mdc format.
