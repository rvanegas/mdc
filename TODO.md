# TODO

- Write tests. The plan called for `tests/test_transcript.py` (preamble parsing,
  pending detection, error cases) and `tests/test_form.py` (slugify, check_file,
  fix_title_section). Integration tests for `run_reply` can use fake clients
  modelled on those in `~/src/mdchat/tests/` if they exist.


